<!--
auto-generated by training/build_paper_section7.py
ckpt: /home/alex/OpenMythos/checkpoints_3b_varT_act_v3/step_0003051_full.pt, step 3051
inputs: depth_extrap_round22.json, act_halt_diagnostic_round22.json, act_halt_histogram_round22.json, gen_samples_round22_multidepth.txt
paste this into docs/paper/main.md, replacing the existing §7 stub
-->

## 7. Empirical results (round 2.1, ACT-bypass training)

Round 2.1 continues from the round-2 final checkpoint, with the
recurrent block's forward monkey-patched to run all `T` iterations and
return `h_T` directly (no ACT weighting). All other settings are held
fixed: variable-T sampling `T_step ∼ U{2, 12}`, FSDP SHARD_GRAD_OP,
bf16 mixed precision, 50M-token target.

### 7.1 Training curves

Loss vs step and per-T loss bands for round 2.1 are in
`figures/round21_training_curve.png` and `figures/round21_loss_by_T.png`.
The training curve continues from where round 2 left off; the per-T plot
shows whether the model now exploits depth (lower loss at higher T) or
remains depth-flat (the round-2 pattern under collapsed ACT).

### 7.2 Depth extrapolation

**FineWeb-Edu held-out cross-entropy at K iterations**, ACT off vs ACT on, round 2.1 vs round 2:

| K  | r2.1 ACT-off | r2 ACT-off | Δ (off) | r2.1 ACT-on | r2 ACT-on | Δ (on) |
|----|--------------|------------|---------|-------------|-----------|--------|
|  4 | 3.8424       | 4.2455     | -0.4031 | 3.9464      | 4.1130    | -0.1666 |
|  8 | 3.8414       | 4.2522     | -0.4108 | 3.9464      | 4.1130    | -0.1666 |
| 16 | 3.8414       | 4.2525     | -0.4111 | 3.9464      | 4.1130    | -0.1666 |
| 32 | 3.8412       | 4.2524     | -0.4112 | 3.9464      | 4.1130    | -0.1666 |

The ACT-off column is the primary read on whether the bypass-trained
backbone learned to leverage depth. Round 2 was already depth-flat at
~4.25 nats with ACT off; the question for round 2.1 is whether the
model now improves with K (loss decreasing as iterations grow) or
remains flat. The ACT-on column reflects whatever the (untrained)
halting head does at inference and is interpretable only in light of
§7.3 below.

_GSM8K table unavailable_

_TinyStories table unavailable_

### 7.3 ACT halting state after bypass training

Round 2 left the ACT head saturated at `p_t = 1.0` for every iteration
and every token (§5). Round 2.1 bypassed the head during training, so
the head received no direct gradient. The diagnostic output below shows
whether the head stayed saturated or drifted off the fixed point through
indirect parameter shifts.

**Per-iteration halting probability at K=4** (threshold = 0.99):

| iter | p_mean | p_median | p_min  | p_max  | cum_p | %≥thr |
|------|--------|----------|--------|--------|-------|-------|
|    1 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.000 |
|    2 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.000 |
|    3 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.000 |
|    4 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 1.000 |

**Halt-step histogram per K**:

| K  | mean halt step | median halt step | tokens at iter 1 | full budget used |
|----|----------------|------------------|------------------|------------------|
|  4 | 1.00           |    1             | 65,536 / 65,536    | 0.000            |
|  8 | 1.00           |    1             | 65,536 / 65,536    | 0.000            |
| 16 | 1.00           |    1             | 65,536 / 65,536    | 0.000            |
| 32 | 1.00           |    1             | 65,536 / 65,536    | 0.000            |

**Verdict.** `p_1 = 1.0000` at K=4, still saturated above 0.95 × threshold. The ACT head was frozen during ACT-bypass training (it received no gradient), so we did not expect it to recover from the round-2 saturation. The evaluation with ACT enabled at inference therefore still produces trivial halting; the meaningful signal is the ACT-off column of the depth-extrapolation table.

### 7.4 Generation samples

First multi-depth generation block (full set in
`docs/gen_samples_round21_multidepth.txt`):

```
# Multi-depth generation samples (step 3051, depths=[2, 6, 12, 24])


## Sample 1

Prompt: `Once upon a time in a small village nestled between two mountains,`


**K=2:** Once upon a time in a small village nestled between two mountains, is located in the same location, and the same day, in a large area of the river called the "Saddam" in the northern region, is located in this area. In fact, the river passes through the area of the river, not only to the east but to the north, but to the north of the river as well the river. The river is about 5 miles or more to the east and to the south and southeast. The river's course is 13 miles from


**K=6:** Once upon a time in a small village nestled between two mountains, is located in the same location, and the same day, in a large area of the r

[truncated; full output in docs/gen_samples_round22_multidepth.txt]
```
